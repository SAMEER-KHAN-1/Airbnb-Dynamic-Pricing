# Claude Code / AI assistant files — keep out of the repo
CLAUDE.md
.claude/

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Raw / large datasets — document download source in README instead
Dataset/
data/raw/

# Processed data / interactions are derived from Dataset/ via src/data_prep.py
# and src/interaction_matrix.py — regenerate, don't commit
data/processed/

# Model artifacts (can be large; regenerate via src/svd_recommender.py and src/content_matcher.py)
models/*.pkl
models/*.joblib

# OS / editor cruft
.DS_Store
Thumbs.db
